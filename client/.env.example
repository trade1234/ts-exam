# For local development, leave empty or set to your local backend:
# VITE_API_URL=http://localhost:5000/api
#
# For Vercel deployment, leave VITE_API_URL empty (frontend and backend
# share the same origin, so the client defaults to /api automatically).
